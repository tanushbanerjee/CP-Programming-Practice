#include <bits.stdc++.h>
using namespace std;

int main()
{
	int tt;
	cin >> tt;
	while(tt--)
	{
		int a;
		int n, k;
		std::vector<int> ar;
		cin >> n >> k;
		cout << "\n";

		for(int i = 0; i < n; ++i)
		{
			cin >> ar[i];
		}

		while(1)
		{
			
		}

	}
	return 0;
}